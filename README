## To Do
* deployment hlf-ca

## Start project
* aws eks update-kubeconfig --region us-east-1 --name meu-cluster-eks
* kubectl apply -f deployments/toolbox.yaml
* install on the toolbox bash-completion, helm, awscli 2, kubectl and you must export envs to aws cli (aws.env)

## install HLF-ca

export PEER_IMAGE=hyperledger/fabric-peer
export PEER_VERSION=3.0.0

export ORDERER_IMAGE=hyperledger/fabric-orderer
export ORDERER_VERSION=3.0.0

export CA_IMAGE=hyperledger/fabric-ca
export CA_VERSION=1.5.13
export SC_NAME=ebs-csi-sc

## Step - Step
* https://github.com/hyperledger-bevel/bevel-operator-fabric?tab=readme-ov-file